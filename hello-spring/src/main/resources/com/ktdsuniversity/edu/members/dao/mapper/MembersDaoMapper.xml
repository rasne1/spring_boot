<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ktdsuniversity.edu.members.dao.MembersDao">

  <insert id="insertNewMembers" 
  parameterType="com.ktdsuniversity.edu.members.vo.request.RegistVO">
  INSERT INTO MEMBERS
     (EMAIL
     , NAME
     , PASSWORD
     , SALT
     , REGIST_DATE
     , LATEST_PASSWORD_CHANGE_DATE
     , LOGIN_FAIL_COUNT
     , LATEST_LOGIN_FAIL_DATE)
     VALUES
     (#{email}
     , #{name}
     , #{password}
     , #{salt}
     , SYSDATE
     , SYSDATE
     , 0
     , SYSDATE)
  </insert>
  
    
   
  <select id="selectMemberList" 
  resultType="com.ktdsuniversity.edu.members.vo.MembersVO">
  SELECT EMAIL
       , NAME
       , PASSWORD
    FROM MEMBERS
  </select>
  
  <select id="selectMemberCount" resultType="_int">
  SELECT COUNT(EMAIL)
    FROM MEMBERS
  </select>
  
  <select id="selectMembersByEmail" resultType="com.ktdsuniversity.edu.members.vo.MembersVO">
  SELECT EMAIL
     , NAME
     , PASSWORD
     , SALT
     , TO_CHAR(REGIST_DATE, 'YYYY-MM-DD HH24:MI:SS') AS REGIST_DATE
     , TO_CHAR(MODIFY_DATE, 'YYYY-MM-DD HH24:MI:SS') AS MODIFY_DATE
     , TO_CHAR(LATEST_PASSWORD_CHANGE_DATE, 'YYYY-MM-DD HH24:MI:SS') AS LATEST_PASSWORD_CHANGE_DATE
     , TO_CHAR(LOGIN_DATE, 'YYYY-MM-DD HH24:MI:SS') AS LOGIN_DATE
     , LATEST_LOGIN_IP
     , LOGIN_FAIL_COUNT
     , TO_CHAR(LATEST_LOGIN_FAIL_DATE, 'YYYY-MM-DD HH24:MI:SS') AS LATEST_LOGIN_FAIL_DATE
     , BLOCK_YN
     , FILE_GROUP_ID
  FROM MEMBERS
 WHERE EMAIL = #{_parameter}
  </select>
  
  
  
  
  <update id="updateMemberByEmail" parameterType="com.ktdsuniversity.edu.members.vo.request.MemberUpdateVO">
  UPDATE MEMBERS
     SET NAME=#{name}
       , PASSWORD=#{password}
     WHERE EMAIL=#{email}
  </update>
  
 
    <update id="updateIncreaseLoginFailCount" 
    parameterType="string">
    UPDATE MEMBERS
       SET LOGIN_FAIL_COUNT = LOGIN_FAIL_COUNT +1
         , LATEST_LOGIN_FAIL_DATE = SYSDATE
     WHERE EMAIL = #{_parameter}
    </update>
    <update id = "updateBlockYn" parameterType="string">
    UPDATE MEMBERS
       SET BLOCK_YN = 'Y'
     WHERE EMAIL = #{_parameter} 
       AND LOGIN_FAIL_COUNT >= 5
    </update>
    <update id = "updateSuccessLogin" parameterType="string">
    UPDATE MEMBERS
       SET LOGIN_FAIL_COUNT = 0
         , LATEST_LOGIN_IP = #{ip}
         , LOGIN_DATE = SYSDATE
         , BLOCK_YN = 'N'
     WHERE EMAIL = #{email}
    </update>
  
  <delete id="deletByEmail" parameterType="string">
    DELETE
          FROM MEMBERS
         WHERE EMAIL = #{_parameter}
  </delete>

  
  
</mapper>